## MoonShine Spring

### Install

1. Install the package via Composer:
    ```shell
    composer require moonshine/spring
    ```
2. Run the command:
    ```shell
    php artisan moonshine-spring:install
    ```
